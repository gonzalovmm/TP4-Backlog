name: CI (Gradle)

on:
  pull_request:
    branches: [develop]   # bloquea el PR hasta pasar checks
  push:
    branches: [develop]


jobs:
  lint_format:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v3
      - name: Checkstyle + Spotless (no modifica código)
        run: |
          ./gradlew checkstyleMain checkstyleTest --no-daemon
          ./gradlew spotlessCheck --no-daemon
  unit_tests:
    runs-on: ubuntu-latest
    needs: [lint_format]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v3
      - name: Unit tests
        run: ./gradlew test --no-daemon
      - name: Publicar reportes (unit)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-report
          path: build/reports/tests/test
      - name: Anotar resultados (unit) en el PR
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: JUnit (unit)
          path: build/test-results/test/*.xml
          reporter: java-junit
          fail-on-error: false

  # integration_tests:
  #   runs-on: ubuntu-latest
  #   needs: [unit_tests]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-java@v4
  #       with: { distribution: temurin, java-version: "17" }
  #     - uses: gradle/actions/setup-gradle@v3
  #     - name: Run integration tests
  #       run: ./gradlew integrationTest --no-daemon
  #     - name: Upload IT report
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: integration-test-report
  #         path: build/reports/tests/integrationTest
  #     - name: Annotate IT results in PR
  #       if: always()
  #       uses: dorny/test-reporter@v1
  #       with:
  #         name: JUnit (integration)
  #         path: build/test-results/integrationTest/*.xml
  #         reporter: java-junit
  #         fail-on-error: false


  build:
    runs-on: ubuntu-latest
    needs: [ unit_tests ] # integration_test a la hora de cambiar
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v3
      - name: Assemble (sin re-ejecutar tests)
        run: ./gradlew assemble --no-daemon
      - name: Publicar artefacto
        uses: actions/upload-artifact@v4
        with:
          name: app
          path: build/libs/*.jar
